SECRET_PATH: "conf/.secrets.yaml"
# Default environment. Overridden if specified through environment variable (prefixed by conf.RTP_ENV_VAR_PREFIX current value)
TARGET_ENV: "uat"
API_KEY_HEADER: "Ocp-Apim-Subscription-Key"
default_timeout: 5000
long_timeout: 60000
iso_date_format: '%Y-%m-%dT%H:%M:%S.000%z'
landing_page_path: "https://rtp.uat.cstar.pagopa.it"
send_rtp_path: "https://api-rtp.uat.cstar.pagopa.it/rtp/v1/rtps"
